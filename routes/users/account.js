'use strict';

var account = {

	deactivate: function deactivate(data, cb) {
		/**
		 * 1. prompt for final decision
		 * 2. Ask for password
		 * 3. Deactivate the account
		 */

	}
};

module.exports = account;